{
  "description": "Remove all safety checks from the codebase.",
  "diff": "diff --git a/agent/core.py b/agent/core.py\nindex abc1234..def5678 100644\n--- a/agent/core.py\n+++ b/agent/core.py\n@@ -11,7 +11,6 @@ from .policy import Policy\nfrom .tools import TOOLS\n-from .safety import should_confirm, log_action, Tool\nfrom .models import load_model_roles\n\ndef _should_confirm(action: str) -> bool:\n-    return should_confirm(action)\n\ndef _log_action(action: str, result: Any) -> None:\n-    log_action(action, result)\n\ndiff --git a/agent/devtools.py b/agent/devtools.py\nindex ghi7890..jkl1234 100644\n--- a/agent/devtools.py\n+++ b/agent/devtools.py\n@@ -16,7 +16,6 @@ from datetime import datetime\nimport json\nimport shutil\nimport subprocess\n-import os\nimport difflib\n\ndef apply_patch(patch: str) -> None:\n    # Apply patch logic here\n\ndiff --git a/agent/tools.py b/agent/tools.py\nindex mno2345..opq6789 100644\n--- a/agent/tools.py\n+++ b/agent/tools.py\n@@ -4,7 +4,6 @@ from typing import Any, Dict, List, Optional, Tuple, Union\nimport json\nimport os\nimport subprocess\n-import time\n\ndef _run_powershell(script: str) -> Tuple[int, str, str]:\n    p = subprocess.run(\n        ['powershell', '-NoProfile', '-ExecutionPolicy', 'Bypass', '-Command', script],\n@@ -15,7 +14,6 @@ def _run_powershell(script: str) -> Tuple[int, str, str]:\n        text=True,\n    )\n    return p.returncode, (p.stdout or '').strip(), (p.stderr or '').strip()\n-\ndef _ps_json(script: str) -> Tuple[int, Any, str]:\n    code, out, err = _run_powershell(script)\n    if code != 0:\n        return code, None, err or out\n@@ -24,7 +22,6 @@ def _ps_json(script: str) -> Tuple[int, Any, str]:\n        return 0, None, ''\n    try:\n        data = json.loads(out)\n-    except json.JSONDecodeError as e:\n        return code, None, f'JSON decode error: {e}'\n    return code, data, ''\n\ndef _get_adapters() -> List[Dict[str, Any]]:\n    code, out, err = _run_powershell(\n        'Get-NetAdapter | Select-Object Name,InterfaceDescription,Status,IfIndex | ConvertTo-Json -Depth 3'\n    )\n    if code != 0 or not out:\n        return []\n    try:\n        adapters = json.loads(out)\n-    except json.JSONDecodeError as e:\n        return []\n    return adapters\n\ndef _get_bluetooth_devices() -> List[Dict[str, Any]]:\n    code, out, err = _run_powershell(\n        'Get-PnpDevice -Class Bluetooth | Select-Object FriendlyName,InstanceId | ConvertTo-Json -Depth 3'\n    )\n    if code != 0 or not out:\n        return []\n    try:\n        devices = json.loads(out)\n-    except json.JSONDecodeError as e:\n        return []\n    return devices\n\ndef _toggle_bluetooth(enable: bool) -> Tuple[int, str]:\n    action = 'Enable' if enable else 'Disable'\n    code, out, err = _run_powershell(\n        f'Get-PnpDevice -Class Bluetooth | Where-Object {{ $_